import Layout from '../../Components/Layout/Layout';
import { FormWrapper } from '../Login/Login.styles';

const Transaction = () => {
  return (
    <Layout>
      <FormWrapper>/</FormWrapper>
    </Layout>
  );
};

export default Transaction;
